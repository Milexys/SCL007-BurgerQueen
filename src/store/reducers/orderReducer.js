const initState = {
    orders: [{
        clientName: 'Milexys',
        food: 'cafe',
    },
    {
        clientName: 'Sara',
        food: 'hamburguesa',
    }],
};

const orderReducer = (state = initState, action) => {
 return state
}

export default orderReducer;