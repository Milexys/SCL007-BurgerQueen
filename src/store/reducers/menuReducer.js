const initState = {
    menus: [{
        clientName: 'Milexys',
        food: 'cafe',
    },
    {
        clientName: 'Sara',
        food: 'hamburguesa',
    }],
};

const menuReducer = (state = initState, action) => {
 return state
}

export default menuReducer;